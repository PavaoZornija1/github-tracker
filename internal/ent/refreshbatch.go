// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/PavaoZornija1/github-tracker/internal/ent/refreshbatch"
	"github.com/google/uuid"
)

// RefreshBatch is the model entity for the RefreshBatch schema.
type RefreshBatch struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RefreshBatchQuery when eager-loading is set.
	Edges        RefreshBatchEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RefreshBatchEdges holds the relations/edges for other nodes in the graph.
type RefreshBatchEdges struct {
	// Jobs holds the value of the jobs edge.
	Jobs []*RefreshBatchJob `json:"jobs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// JobsOrErr returns the Jobs value or an error if the edge
// was not loaded in eager-loading.
func (e RefreshBatchEdges) JobsOrErr() ([]*RefreshBatchJob, error) {
	if e.loadedTypes[0] {
		return e.Jobs, nil
	}
	return nil, &NotLoadedError{edge: "jobs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RefreshBatch) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case refreshbatch.FieldCreatedAt, refreshbatch.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case refreshbatch.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RefreshBatch fields.
func (_m *RefreshBatch) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case refreshbatch.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case refreshbatch.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case refreshbatch.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RefreshBatch.
// This includes values selected through modifiers, order, etc.
func (_m *RefreshBatch) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryJobs queries the "jobs" edge of the RefreshBatch entity.
func (_m *RefreshBatch) QueryJobs() *RefreshBatchJobQuery {
	return NewRefreshBatchClient(_m.config).QueryJobs(_m)
}

// Update returns a builder for updating this RefreshBatch.
// Note that you need to call RefreshBatch.Unwrap() before calling this method if this RefreshBatch
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RefreshBatch) Update() *RefreshBatchUpdateOne {
	return NewRefreshBatchClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RefreshBatch entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RefreshBatch) Unwrap() *RefreshBatch {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RefreshBatch is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RefreshBatch) String() string {
	var builder strings.Builder
	builder.WriteString("RefreshBatch(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RefreshBatches is a parsable slice of RefreshBatch.
type RefreshBatches []*RefreshBatch
